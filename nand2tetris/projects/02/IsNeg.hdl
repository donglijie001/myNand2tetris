/**
判断负数，负数是按照补码存储的，所以最高位只能是1，否则就不是负数
 */
CHIP IsNeg {
    IN in[16];
    OUT out;

    PARTS:
   // Or(a = in[15], b = false, out = out);
   // 用and判断也是可以的。
    And(a = in[15], b = true, out = out);

}